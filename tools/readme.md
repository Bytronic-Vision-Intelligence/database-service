# Tools

This directory is reserved for operational scripts and database utilities.
Run tools from the repository root so imports and paths resolve consistently.

The service itself is started with:

```powershell
python app/main.py
```

For setup and MQTT payload examples, see the repository [README](../Readme.md).
