[pytest]
# `.` supports `from app.dependencies... import ...`, used by the existing tests.
# `app` supports `from main import ...` and `from dependencies... import ...`,
# exactly how the code imports at runtime.
pythonpath = . app
testpaths = test
